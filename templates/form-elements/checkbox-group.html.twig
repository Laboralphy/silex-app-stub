<div class="row field-element">
    <div class="col lg-12"><label>{{ label }}</label></div>
    <div class="col lg-12">
        {% set i = 0 %}
        {% for opt in options %}
            {% include 'form-elements/checkbox.html.twig' with {
                'name': name ~ '[]',
                'label': opt.label,
                'value': opt.value,
                'checked': opt.checked | default(false)
            } %}
            {% set i = i + 1 %}
        {% endfor %}
    </div>
</div>
